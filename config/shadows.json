{
  "_schema_documentation": {
    "description": "Registry of shadow strategies for A/B testing against the live (champion) strategy. Each entry has the keys below. Set 'active': false to keep an entry as documentation without running it.",
    "max_active": 3,
    "fields": {
      "active":            "boolean. If false, the shadow is documented but not evaluated.",
      "type":              "Implementation type. Must be a key in SHADOW_IMPLEMENTATIONS. Currently: 'hold_through_rerank', 'rank_window', 'prior_week_filter'.",
      "description":       "Free-text description of what the shadow is testing.",
      "active_since":      "YYYY-MM-DD. Only weeks on or after this date are evaluated. Lets you start tracking a hypothesis from a specific cut date.",
      "params":            "Type-specific parameter overrides (see comments per type).",
      "promotion_criteria": "Free-text reference to the pre-registered markdown doc with the promote/retire decision criteria. Required reading before promoting."
    },
    "shadow_types": {
      "hold_through_rerank": {
        "behavior":  "Each week N's top-N picks: hold through weeks N+1, N+2,... as long as the name still ranks in the top-N. Exit at the close of the week it drops out.",
        "data_required": "Just perf_log."
      },
      "rank_window": {
        "behavior":  "Pick top-N from a sliding window of composite_rank positions. E.g. start_rank=4 + top_n=10 means use ranks 4-13.",
        "data_required": "Just perf_log."
      },
      "prior_week_filter": {
        "behavior":  "Filter top-N picks by prior-week return bounds. Picks must satisfy min_prior_return <= return_fri_fri <= max_prior_return.",
        "data_required": "perf_log with return_fri_fri column."
      }
    }
  },

  "shadow_hold_through_rerank_v1": {
    "active": false,
    "type": "hold_through_rerank",
    "description": "Hold positions through future weeks if they re-rank in top-10 (composite). Initial 6-week backtest showed +0.32%/wk delta vs champion, win rate 33%, p=0.65. Effect is small and noisy. Activation deferred until consecutive-top-10 events accumulate.",
    "active_since": "2026-04-17",
    "params": {
      "top_n": 10,
      "rank_col": "composite_rank",
      "max_extension_weeks": 4
    },
    "promotion_criteria": "docs/shadow_criteria/hold_through_rerank_v1.md (to be written when activating)"
  },

  "shadow_skip_top_3_v1": {
    "active": false,
    "type": "rank_window",
    "description": "Skip composite ranks 1-3, use ranks 4-13 instead. Tests the rank-crowding hypothesis. Initial 6-week backtest: +1.11%/wk delta vs champion, win rate 83% (beats 5/6 weeks). CI [-1.28%, +2.99%] still straddles zero at n=6 but direction is consistent. Structural support: ranks 1-3 have median -7.3% prior-week return, suggesting composite scoring is biased toward mean-reversion candidates.",
    "active_since": "2026-04-17",
    "params": {
      "start_rank": 4,
      "top_n": 10,
      "rank_col": "composite_rank"
    },
    "promotion_criteria": "docs/shadow_criteria/skip_top_3_v1.md (to be written when activating)"
  },

  "shadow_v1_prior_week_filter_v1_DEPRECATED": {
    "active": false,
    "type": "prior_week_filter",
    "description": "DEPRECATED -- 6-week backtest showed -3.79%/wk delta vs champion (worse), p=0.04, win rate 17%. The V1 bounds (-20% to +10%) do not work as filter for V2's Monday-close entry timing. Likely reasons: (a) V2 model already encodes prior-week behavior in composite scoring; (b) V2 entry timing requires different bounds. Could revisit with reversed asymmetry (e.g. -10% to +20%) as a separate shadow.",
    "active_since": "2026-04-17",
    "params": {
      "top_n": 10,
      "rank_col": "composite_rank",
      "min_prior_return": -0.20,
      "max_prior_return": 0.10,
      "max_search_depth": 50
    },
    "promotion_criteria": "n/a -- deprecated, see backtest result above"
  }
}
